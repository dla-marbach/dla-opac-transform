# title (Jahresangabe aus dateLifespanStart-Jahresangabe aus dateLifespanEnd)
- op: core/column-addition
  engineConfig:
    facets: []
    mode: row-based
  baseColumnName: id
  expression: |
    grel:
    forNonBlank(
      cells['title'].value,
      x,
      x,
      'ohne Titel'
    )
    + forNonBlank(
      forNonBlank(
        cells['dateLifespanStart'].value,
        x,
        x[0,4],
        ''
      )
      + if(
          or(
            isNonBlank(cells['dateLifespanStart'].value),
            isNonBlank(cells['dateLifespanEnd'].value)
          ),
          '-',
          ''
        )
      + forNonBlank(
        cells['dateLifespanEnd'].value,
        x,
        x[0,4],
        ''
      ),
      x,
      ' (' + x + ')',
      ''
    )
  newColumnName: display
  columnInsertIndex: 1
